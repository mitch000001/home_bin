#! /bin/bash

_vmrun()
{
  local current previous options
  COMPREPLY=()
  current="${COMP_WORDS[COMP_CWORD]}"
  previous="${COMP_WORDS[COMP_CWORD-1]}"
  #  The basic options we'll complete.
  options="start stop list"

  #
  #  Complete the arguments to some of the basic commands.
  #
  case "${previous}" in
    start)
      local names=`ls -1 $HOME/Documents/Virtual_Machines.localized/*/*.vmx`;
      COMPREPLY=( $(compgen -W "${names}" -- ${current}) )
      return 0
      ;;
    stop)
      local running_machines=`vmrun list | grep '.vmx'`;
      COMPREPLY=( $(compgen -W "${running_machines}" -- ${current}) );
      return 0;
      ;;
    vmrun)
      COMPREPLY=( $(compgen -W "${options}" -- ${current}) )
      return 0
      ;;
    *)
      return 0
      ;;
  esac

}
complete -F _vmrun vmrun
